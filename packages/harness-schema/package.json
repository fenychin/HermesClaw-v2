{
  "name": "@hermesclaw/harness-schema",
  "version": "0.13.0",
  "private": true,
  "description": "HermesClaw Harness Runtime objects (HarnessBundle / WorkflowTemplate / AgentPolicy / SkillBinding / ContextPolicy / MemoryPolicy / ConnectorPolicy / EvalRuleSet) + HarnessProposal / EvolutionProposal / EvaluationReport / IndustryManifest.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config ../../vitest.config.ts --dir __tests__"
  },
  "dependencies": {
    "@hermesclaw/event-contracts": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.8"
  }
}
