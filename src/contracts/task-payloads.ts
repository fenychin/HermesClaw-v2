/**
 * @deprecated 直接 import 自 `@hermesclaw/event-contracts`。
 * 本 shim 保留 `@/contracts/task-payloads` 子路径兼容性（Bundler 模块解析），
 * 将在 v0.13+ monorepo 拆分时移除。
 */
export * from "../../packages/event-contracts/src/task-payloads"
